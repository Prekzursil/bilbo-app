<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility service configuration for the Bilbo github flavor.
    This is only used when USE_ACCESSIBILITY_SERVICE build config flag is true.
    The service monitors window state changes to determine which app is in the foreground.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagIncludeNotImportantViews|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
